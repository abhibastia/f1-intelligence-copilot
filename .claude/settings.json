{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.py) ruff check --fix \"$f\" 2>/dev/null; ruff format \"$f\" 2>/dev/null;; esac; } || true"
          }
        ]
      }
    ]
  }
}

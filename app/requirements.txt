--extra-index-url https://download.pytorch.org/whl/cpu
flask>=3.0.0
# agent.py posts to the serving endpoint directly. It currently arrives via
# databricks-sdk, but an undeclared import is one dependency bump from breaking.
requests>=2.31.0
# Pure-Python driver, not psycopg2-binary — see f1lake/schema.py.
pg8000>=1.31
databricks-sdk>=0.30.0
sentence-transformers>=3.0.0

# CPU-only torch: the CUDA build is ~2.5 GB and would risk the app image
# timing out on Free Edition.
--extra-index-url https://download.pytorch.org/whl/cpu
fastmcp>=3.2.0
uvicorn>=0.30.0
# Pure-Python driver, not psycopg2-binary — see f1lake/schema.py.
pg8000>=1.31
databricks-sdk>=0.30.0
sentence-transformers>=3.0.0
